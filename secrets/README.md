# Local secrets

Create a file named `bitcoin_rpc_password` in this directory containing only
the plain-text password for the read-only dashboard RPC user.

The password file is ignored by Git. Keep this README as the only tracked file
in the directory and set the secret file to mode `0600`.
